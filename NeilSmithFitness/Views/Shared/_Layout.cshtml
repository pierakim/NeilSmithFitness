<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/svg" href="~/Content/images/Logos/NeilSmith_Logo.NoText.Black.ico">
    <title>Neil Smith | Fitness</title>
    @*CSS Bundle*@
    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Alfa+Slab+One" rel="stylesheet">
    @Styles.Render("~/bundles/css")

</head>
<body>

<nav class="navbar navbar-inverse" role="navigation">
    <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav navbar-left">
            <li><a href="#aboutMe">About Me</a></li>
            <li><a href="#buildMuscle">Build Muscle</a></li>
            <li><a href="#burnFat">Burn Fat</a></li>
            <li><a href="#photoCarroussel">Programs</a></li>
            <li><a href="#contactPanelContainer">Contact</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li><a target="_blank" href="https://www.instagram.com/neilsmith1984/">Instagram</a></li>
            <li><a href="#">Facebook</a></li>
            <a class="navbar-brand" href="#">
                <img src="~/Content/images/Logos/NeilSmith_Logo.NoText.White.svg" class="img-responsive">
            </a>
        </ul>
    </div>
</nav>

@RenderBody()
<footer>
    <div class="container">
        <div>
            <p>&copy; @DateTime.Now.Year - www.NeilSmithFitness.com</p>
        </div>
    </div>
</footer>

@*JS Bundle*@
@Scripts.Render("~/bundles/js")

</body>
</html>


@***************@
@*Effect Script*@
@***************@

@*CAROUSSEL PHOTO GALLERY (auto run)*@
<script>
  $(function() {
    $(".rslides").responsiveSlides();
  });
</script>

@*MODAL POP UP*@
<script>
    $(document)
        .ready(function() {
            // Show Update Success modal popup
            if ($('#showSuccessMessage').text().length > 0) {
                $('#successModal').modal({ backdrop: 'static' });
            }
        });
</script>

@*SPINNER LOADING - Submit contact details*@
<script>
    $(function () {
        $("#submitContact").click(function () {
            debugger;
            if ($("#frmContact").valid()) {
                $("#loading").fadeIn();
                var opts = {
                    lines: 12, // The number of lines to draw
                    length: 7, // The length of each line
                    width: 4, // The line thickness
                    radius: 10, // The radius of the inner circle
                    color: '#000', // #rgb or #rrggbb
                    speed: 1, // Rounds per second
                    trail: 60, // Afterglow percentage
                    shadow: false, // Whether to render a shadow
                    hwaccel: false // Whether to use hardware acceleration
                };
                var target = document.getElementById('loading');
                var spinner = new Spinner(opts).spin(target);
            }
        });
    });
</script>

@*Google Map for house location*@
<script>
    function initMap() {
        var uluru = {lat: -25.363, lng: 131.044};
        var map = new google.maps.Map(document.getElementById('map'), {
            zoom: 4,
            center: uluru
        });
        var marker = new google.maps.Marker({
            position: uluru,
            map: map
        });
    }
</script>
<script async defer
        src="https://maps.googleapis.com/maps/api/js?key=AIzaSyDbunbKx8vsHKrNWwhJlsZmSCDawGDeqY8&callback=initMap">
</script>

@*AOS special effect framework*@
<script>
    AOS.init({
        //disable: window.innerWidth < 1024
    });
</script>

@*STELLAR*@
<script>
    $(document)
        .ready(function() {
            $.stellar({
                horizontalScrolling: false,
                responsive: true
            });
        });
</script>

@*Vertical side scroll*@
<script>
    $(document).ready(function ($) {
        //scrolling on click
        $('a[href*="#"]:not([href="#"])').click(function () {
            if (location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') && location.hostname == this.hostname) {
                var target = $(this.hash);
                target = target.length ? target : $('[name=' + this.hash.slice(1) + ']');
                var id = target[0].id;
                var ratio = id == "aboutMe" || id == "photoCarroussel" ? $(window).height() / 4 : 0;
                if (target.length) {
                    $('html, body').animate({
                        scrollTop: target.offset().top - ratio
                    }, 1500);
                    return false;
                }
            }
        });

        //show/hide label
        $('.vNav a').hover(function () {
            $(this).find('.label').show();
        }, function () {
            $(this).find('.label').hide();
        });
    });
</script>